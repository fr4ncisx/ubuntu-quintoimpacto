## DATABASE ##
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Fill with postgres password
# Only used by Docker Compose healthcheck, not by the app:
POSTGRES_DB=ubuntu
# In Docker Compose this is overridden to jdbc:postgresql://postgres:5432/ubuntu
POSTGRES_URL=jdbc:postgresql://localhost:5432/ubuntu

## DATA SEEDING (CommandLineRunner seeders; compose overrides to false) ##
APP_SEEDING_ENABLED=true

## GOOGLE OAUTH2 (Authorization Code Flow; both required; configure the redirect URI in Google Console) ##
GOOGLE_CLIENT_ID=Fill with Google client id
GOOGLE_CLIENT_SECRET=Fill with Google client secret
FRONTEND_POST_LOGIN_URL=Fill with frontend URL for post-login redirect

## CORS ##
FRONTEND_IP=http://ip:port
PROD_IP=Fill with production URL
# Optional, defaults to http://localhost:5173 in application.yml
FRONTEND_LOCAL=http://localhost:5173

## JWT ##
SECRET_KEY=Fill with a random generated key 256 bit

## CLOUDINARY ##
CLOUDINARY=cloudinary://PUBLIC API KEY:SECRET API KEY@CLOUD NAME

## EMAIL (requires APP_MAIL_ENABLED=true and a Gmail app password) ##
APP_MAIL_ENABLED=false
EMAIL_SENDER=Fill with sender email address
EMAIL_SENDER_PASSWORD=Fill with sender app password

## NOMINATIM (optional, defaults to public OpenStreetMap endpoints in application.yml) ##
NOMINATIM_SEARCH=https://nominatim.openstreetmap.org/search
NOMINATIM_REVERSE=https://nominatim.openstreetmap.org/reverse
